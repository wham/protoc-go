See [AGENTS.md](AGENTS.md) — it is the single source of truth for how to work in
this repo (build and test commands, layout, conventions). Everything there
applies to Claude Code sessions too; nothing is duplicated here so the two
cannot drift.
