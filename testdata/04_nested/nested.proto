syntax = "proto3";

package nested;

message Outer {
  message Inner {
    string value = 1;
  }

  Inner inner = 1;
  string name = 2;

  enum Status {
    STATUS_UNSPECIFIED = 0;
    STATUS_ACTIVE = 1;
    STATUS_INACTIVE = 2;
  }

  Status status = 3;
}
