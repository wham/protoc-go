syntax = "proto3";

package multierrenummsg;

enum Color {
  RED = 0;
  GREEN = 1;
  RED = 2;
}

message Outer {
  string name = 1;

  message Inner {
    string value = 1;
    string value = 2;
  }

  enum Color {
    UNKNOWN = 0;
  }
}

message Outer {
  int32 id = 1;
}
