syntax = "proto2";

package intenumd;

enum Priority {
  LOW = 0;
  MEDIUM = 1;
  HIGH = 2;
}

message Task {
  optional Priority level = 1 [default = 0];
  optional Priority urgency = 2 [default = 2];
}
