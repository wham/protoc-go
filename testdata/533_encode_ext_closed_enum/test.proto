syntax = "proto2";

package encextcenum;

enum Priority {
  LOW = 0;
  HIGH = 1;
}

message Inner {
  optional int32 priority = 1;
}

message Outer {
  optional Priority priority = 1;
  optional string label = 2;
  extensions 100 to 200;
}

extend Outer {
  optional Inner my_ext = 100;
}
