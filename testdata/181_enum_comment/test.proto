syntax = "proto3";

package enumcomment;

// The status of a task in the system.
enum Status {
  UNKNOWN = 0;
  ACTIVE = 1;
  INACTIVE = 2;
}

message Task {
  string name = 1;
  Status status = 2;
}
