syntax = "proto2";

package evneginf;

import "google/protobuf/descriptor.proto";

extend google.protobuf.EnumValueOptions {
  optional double val_threshold = 50001;
}

enum Priority {
  LOW = 0 [(val_threshold) = -inf];
  HIGH = 1;
}

message Holder {
  optional Priority p = 1;
}
