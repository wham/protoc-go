edition = "2023";

package enumjson;

enum Status {
  option features.json_format = ALLOW;
  STATUS_UNKNOWN = 0;
  STATUS_ACTIVE = 1;
  STATUS_INACTIVE = 2;
}

message Config {
  Status status = 1;
}
