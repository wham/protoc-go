syntax = "proto3";

package enum_reserved_example;

enum Status {
  STATUS_UNKNOWN = 0;
  STATUS_ACTIVE = 1;
  STATUS_INACTIVE = 4;
  reserved 2, 3;
  reserved 10 to 20;
  reserved "DELETED", "ARCHIVED";
}
