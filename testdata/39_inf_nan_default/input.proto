syntax = "proto2";

package infnan;

message Limits {
  optional double pos_inf = 1 [default = inf];
  optional double neg_inf = 2 [default = -inf];
  optional double not_a_number = 3 [default = nan];
  optional float float_inf = 4 [default = inf];
  optional float float_nan = 5 [default = nan];
}
