syntax = "proto2";

package duptest;

import "google/protobuf/descriptor.proto";

extend google.protobuf.MessageOptions {
  optional int32 msg_tag = 50001;
}

message MyMessage {
  option (msg_tag) = 42;
  option (msg_tag) = 99;

  optional string name = 1;
}
