syntax = "proto3";

package methodbody;

message Request {
  string query = 1;
}

message Response {
  string result = 1;
}

service SearchService {
  rpc Search(Request) returns (Response) {
    string invalid_field = 1;
  }
}
