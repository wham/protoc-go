syntax = "proto3";

package unknownmethopt;

message Request {
  string query = 1;
}

message Response {
  string result = 1;
}

service SearchService {
  rpc Search(Request) returns (Response) {
    option foobar = true;
  }
}
