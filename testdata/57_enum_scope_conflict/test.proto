syntax = "proto3";

package enumscope;

enum Color {
  UNKNOWN = 0;
  RED = 1;
  GREEN = 2;
}

enum Priority {
  UNKNOWN = 0;
  HIGH = 1;
  LOW = 2;
}

message Config {
  Color color = 1;
  Priority priority = 2;
}
